# Karaoke Management API

## RUN command to install library needed

npm i

## RUN these commands to fake some tables

### Create all tables

npx sequelize-cli db:migrate

### Create seeder

npx sequelize-cli db:seed:all
